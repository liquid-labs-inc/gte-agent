# Historical OpenCode Context

This directory contains inherited OpenCode specs and migration notes that were not promoted into active GTE Agent planning during Milestone 1.

Use these files only as historical reference. Active GTE Agent planning lives in `docs/overview.md` and `docs/strip-plan.md`.
